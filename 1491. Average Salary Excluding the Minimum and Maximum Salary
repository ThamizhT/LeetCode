class Solution:
    def average(self, salary: List[int]) -> float:
        min_s=min(salary)
        max_s=max(salary)
        total_s=sum(salary)-min_s-max_s
        emp=len(salary)-2
        result=total_s / emp
        return result
